<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>
      {{#if title}}
        {{title}}
      {{else}}
        LTI Seed App
      {{/if}}
    </title>
</head>
<body>
    <header>
        {{> header }}
    </header>
    {{{body}}}
    <footer>
        {{> footer }}
    </footer>
</body>
</html>